// Firebase's web configuration is public but deployment-specific. Do not put
// service-account credentials, private keys, or server tokens in this file.
// Before deploying web push, replace the two placeholders with the Firebase
// CDN versions and the public Firebase web configuration for this environment.
//
// importScripts('https://www.gstatic.com/firebasejs/<VERSION>/firebase-app-compat.js');
// importScripts('https://www.gstatic.com/firebasejs/<VERSION>/firebase-messaging-compat.js');
// firebase.initializeApp({ /* public Firebase web config */ });
// firebase.messaging();
